#include <stdio.h>

#define MAX 20
# define LETRA 999

int sopa [MAX][MAX];
int m,n,k,num=0;
char letra[LETRA];

scanf("%d %d",&m,&n);

for (int i=0;i<m;i++){
  for(int j=0;j<n && (sopa[j]= getchar()) != '\n';j++)
}

scanf("%d",&k);

while (num < k){
  fgets(letra,LETRA,stdin);



  n++;
}
